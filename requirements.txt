# Core runtime dependencies for btc-bot v1.0.
# Keep this list explicit and pinned during stabilization.
requests>=2.32.0,<3.0.0
websockets>=12.0,<16.0
optuna>=3.6.0,<5.0.0
yfinance>=0.2.52,<1.0.0
PySocks>=1.7.1,<2.0.0  # SOCKS5 proxy support
psutil>=5.9.0,<7.0.0  # Server resource monitoring (dashboard only)
