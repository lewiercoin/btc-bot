# DEPRECATED

Files in this directory are historical research drafts and are NOT source of truth
for the current codebase.

They reference non-existent config classes (FeatureEngineConfig, RegimeEngineConfig,
SignalEngineConfig, GovernanceConfig) that do not exist in the current API.

The current API uses: AppSettings, StrategyConfig, RiskConfig (all in settings.py).

New implementation: research_lab/ (root of repo)
